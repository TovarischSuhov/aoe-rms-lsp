# task-xs-comments: xs — экспорт комментариев + аудит AST (#119)

Слот 2 пачки v1-final-mile (`.goga/history/2026/v1-final-mile/task.md`), лист.
Зеркало — issue #119 (PR закрывает `Fixes #119`).

## Current State

- `internal/xs`: сканер собирает `noncode []common.Range` (`parse.go:1418`) —
  в одном потоке экстенты `//` (`parse.go:1452`), `/* */` (`parse.go:1459`) и
  строковых токенов (`parse.go:1479`, `tok.at` от `xString`). Поле
  неэкспортируемое — наружу XS-комментарии не видны, потребитель отличить
  комментарий от строки не может.
- `noncode` читают только для «тишины» внутри строк и комментариев:
  `CallAt` (`ast.go:170`) и `VisibleAt` (`ast.go:674`); через них — семантика
  `EnclosingRanges` и `ReferencesAt`/`RenameSites` (`ast.go:246`, `666`).
- Прецедент RMS (#45, коммиты `59756db`→`ce6d07c`): `RmsFile.Comments
  []common.Range` — точные экстенты (блок от `/*` до `*/`, строчный до конца
  строки), сортировка по позиции, непересечение, тексты не хранятся
  (потребитель читает источник по `Offset`); контракт —
  `internal/rms/CODEMANIFEST:132`, тесты — `internal/rms/comments_test.go`.
  У XS аналога нет.
- Потребитель — будущий XS-принтер (#120, слот 3): печать `.xs` требует
  якорения комментариев по экстентам, как это уже делает RMS-принтер
  (`internal/format/rms.go:419-533` — список `comments` + индекс `pending`,
  `flushBefore`/`dropBefore`).
- Черновая разведка потерь AST (проверена по коду, уточняется аудитом):
  - модификаторы `rule` (`inactive`, `min-interval N`) проглатываются
    (`parse.go:260-262`) — в AST их нет, при этом `Decl.Range` их покрывает;
  - голый блок как выражение → `Expr{Kind: literal, Value: "{}"}`, `Range` —
    только открывающая `{`, тело блока в диапазон не входит (`parse.go:1013`);
  - скобки группировки исчезают: `parseParenExpr` возвращает внутреннее
    выражение, `Range` скобки не покрывает (`parse.go:1032-1036`);
  - лексерные: `1e5` сканируется как число `1` + идентификатор `e5`, `1.` —
    как число `1` + оператор `.` (`scanNumber`, `parse.go:1536+`);
  - тексты литералов при этом сохраняются дословно (`Expr.Value = tok.text`,
    `parse.go:1000`) — int/float/hex и escape-последовательности не теряются.

## Description

Экспорт XS-комментариев и сплошной аудит AST как вход для дизайна XS-принтера
(#120).

1. Разделить `xscanner.noncode` на два потока: комментарии (`//`, `/* */`) и
   строковые токены. Комментарии — экспортируемое `XsFile.Comments
   []common.Range` с гарантиями RMS-прецедента; строки остаются внутренними и
   продолжают питать «тишину» `CallAt`/`VisibleAt`/`EnclosingRanges`.
2. Записать `Comments` в `internal/xs/CODEMANIFEST` (модификация публичной
   поверхности — через goga-change flow, не read-only правка).
3. Аудит: сплошной обход видов деклараций (function/variable/rule/event/
   include/extern), стейтментов (все `Stmt*`) и выражений (все `Expr*`) плюс
   лексерных решений — что узел теряет относительно исходного текста: годится
   ли он для дословной печати и что принтеру придётся синтезировать. Находки —
   разделом `## Результаты` в этот же task.md.

Целевой вызов (якорь для #120; имена не фиксируются):

```go
file, _ := xs.XsParse(src, "a.xs")
for _, r := range file.Comments {
	text := src[r.Start.Offset:r.End.Offset] // тексты в AST не хранятся
}
```

## Scope

**In scope:**

- `internal/xs`: разделение смешанного `noncode` на комментарии и строки;
  экспорт `XsFile.Comments`; сохранение семантики «тишины» для
  `CallAt`/`VisibleAt`/`EnclosingRanges`/`ReferencesAt`
- table-driven тесты на экстенты: `//` до конца строки (включая последнюю
  строку без `\n`), `/* */` многострочный от `/*` до `*/`, `//` и `/*` внутри
  строкового литерала — не комментарии, комментарий внутри выражения и списка
  аргументов, порядок по позиции и непересечение, отсутствие строковых
  токенов в `Comments`
- `internal/xs/CODEMANIFEST`: свойство `Comments -> []Range` у `XsFile` с
  гарантиями (по образцу rms)
- аудит AST сплошным обходом — раздел `## Результаты` в этом task.md

**Out of scope:**

- XS-принтер и `format.XS`, правки `internal/format` (слот 3, #120)
- правки AST/парсера по находкам аудита (скобки группировки, модификаторы
  rule, голый блок) — только фиксация; решение об их правке принимается в #120
- серверный хендлер для `.xs`-документов (слот 3)
- правки RMS-стороны (экспорт комментариев там уже сделан в #45)

## Acceptance Criteria

- `XsFile.Comments` экспортирован: точные экстенты, сортировка по позиции,
  непересечение, тексты не хранятся (адресация через `Offset` источника)
- смешанного `noncode` не осталось: комментарии и строки — разные потоки
- «тишина» в строках и комментариях не изменилась — существующие тесты
  навигации XS (`navigation_test.go`) зелёные без правок семантики
- тесты на экстенты комментариев (перечень случаев из Scope) добавлены и
  зелёные; строковые токены в `Comments` не попадают
- `internal/xs/CODEMANIFEST` описывает `Comments` с гарантиями
- раздел `## Результаты` покрывает все виды `Decl`/`Stmt`/`Expr` и лексерные
  решения, каждая находка подтверждена по коду или тестом
- `make check` зелёный, `goga contract xs` зелёный, CI ветки зелёный

## Stack

- **Frameworks:** —
- **Libraries:** stdlib Go 1.26+ (новых зависимостей нет)
- **Infrastructure:** —

## External Dependencies

| Component | Usage file | Status |
|-----------|------------|--------|
| — (внешних нет) | — | — |

Новых внешних зависимостей нет; обновление cooks не требуется (по плану
пачки `lsp-protocol` трогают слоты 1 и 3).

## Risks and Constraints

- «Тишина» сейчас опирается на один поток `noncode`: после разделения
  `CallAt`/`VisibleAt` обязаны видеть оба (комментарии + строки), иначе
  позиция внутри строки начнёт отвечать как код — регресс навигации.
  Страховка — существующие тесты `navigation_test.go` (случаи in-string и
  in-comment).
- Экспорт строк в `Comments` недопустим: принтер заякорит строковый литерал
  как комментарий и напечатает его дважды.
- `XsParse` вызывается и на inline-XS из RMS-блоков: позиции и `Offset`
  относительны строки блока, а не файла RMS. Печатается inline-XS вербатим
  (решение сессии #45), поэтому конфликта нет — но ограничение фиксируется в
  контракте.
- Аудит без правок: находки могут потребовать изменения AST в #120 —
  объём той задачи оценивается отдельно, здесь ничего не «закладывается».
- Тесты — только под memory cap (`go test` в cgroup-песочнице, см. CLAUDE.md).

## Scope Estimate

Единая задача (лист, size S-M по issue #119): одна клетка (`xs`) — правка
сканера + контракт + тесты + отчёт-аудит. Декомпозиция не требуется.

## Existing Architecture

- `internal/xs` — modify: `xscanner.noncode` → `comments`/`strings`;
  `XsFile.noncode` → экспортируемое `Comments` + внутренние строки;
  потребители «тишины» — методы `XsFile` (`ast.go`)
- `internal/format` — будущий потребитель (слот 3, #120): якорение
  комментариев по образцу RMS-принтера
- `internal/rms` — прецедент контракта и тестов (`comments_test.go`),
  не меняется
- прочие клетки (`include`, `analysis`, `server`, `corpus`) не затрагиваются

## Notes

- Решения propose-сессии 2026-09-25 (пользователь): границы — только фиксация
  находок аудита, без правок AST; отчёт аудита живёт в этом task.md; глубина
  аудита — сплошной обход по видам узлов; слот остаётся листом
- Порядок пачки: этот слот идёт до #120 — принтер опирается и на экспорт
  комментариев, и на карту потерь AST
- Формулировка issue #119 («Split the unexported XsFile.noncode … mirroring
  the rms Comments change of #45; audit what the XS AST loses … as input for
  the XS printer design») разворачивается этим task.md без изменения объёма
